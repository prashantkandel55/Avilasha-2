# PocketBase Configuration
VITE_POCKETBASE_URL=http://localhost:8090

# Electron Configuration
NODE_ENV=development

# Note: Create a .env file with your actual configuration
# PocketBase is running locally by default at http://localhost:8090
